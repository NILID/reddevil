= form_for [year, purchase], remote: true, html: { class: 'form-horizontal' } do |f|
  .modal-body

    .control-group
      = f.label attr, class: 'control-label'
      .controls= f.text_field attr, class: 'text_field'

  .modal-footer
    %button.btn(data-dismiss='modal' aria-hidden='true')= t 'shared.close'
    = f.submit nil, class: 'btn btn-primary'
