- if object.errors.any?
  #error_explanation
    .h4= t 'shared.errors_done', error: plural(object.errors.count, 'error_done')
    .alert.alert-danger(role='alert')
      - object.errors.full_messages.each do |msg|
        %p.my-0= msg
