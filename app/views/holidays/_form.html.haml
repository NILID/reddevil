= bootstrap_form_for [@member, @holiday], layout: :horizontal do |f|
  = f.date_select :start, :class => 'date_select'
  = f.date_select :end, :class => 'date_select'
  = f.form_group do
    = f.primary
    = link_to t('shared.cancel'), [@member, Holiday], :class => 'btn btn-default'
