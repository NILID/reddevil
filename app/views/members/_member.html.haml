%p{ class: bdclass }
  - if bdclass
    %i.glyphicon.glyphicon-gift(style="font-size:20px;margin-right:10px;")
  = l member.birth,  format: :very_short
  \ -
  = member.full_name
  (#{turntext + ' ' + plural((member.age + (should_be_age ? 1 : 0)), 'year')})