!!!
%html
  %head
    %meta{ charset: 'utf-8'}
    :css
      body {font-size:12px;font-family: Geneva, Arial, Helvetica, sans-serif; color: #333}
      .title {font-size:18px;text-align:center;margin-bottom:10px;}
      table {width:100%;border-collapse:collapse;}
      table, tr, td{border:1px solid #ddd;}
      td{padding: 5px}
      th{white-space:nowrap}
      .warning{background:#eee}
      .error{background:#aaa}
      .info{background:#ccc}
      .success {background:#fff}
      .timestamp {font-size:8px;position:absolute;right:0}
  %body
    .timestamp= l DateTime.now
    .title= t 'substrates.title'
    %table
      %thead
        %tr
          %th №
          %th= Substrate.human_attribute_name(:title)
          %th= Substrate.human_attribute_name(:drawing)
          %th= Substrate.human_attribute_name(:number)
          %th= Substrate.human_attribute_name(:state)
          %th.big-tr= Substrate.human_attribute_name(:desc)
          %th= Substrate.human_attribute_name(:user_id)
      %tbody= render @substrates
