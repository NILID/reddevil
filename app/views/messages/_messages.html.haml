- if @messages
  .row
    .col-md-12
      - @messages.each do |message|
        .alert.alert-primary(role='alert')
          - if message.title?
            %h4.alert-heading= message.title
          = message.content
