.page-header
  %h3=t 'profiles.editing'

= form_for [@user, @profile], html: {layout: 'form-horizontal'} do |f|
  = f.label :login
  = f.text_field :login
  = f.label :avatar
  = f.file_field :avatar
  .form-actions= f.submit
