%h1 Listing columns

%table
  %tr
    %th Name
    %th Type
    %th Table
    %th
    %th
    %th

  - @columns.each do |column|
    %tr
      %td= column.name
      %td= column.column_type
      %td= column.table
      %td= link_to 'Show', [@table, column]
      %td= link_to 'Edit', edit_table_column_path(@table, column)
      %td= link_to 'Destroy', [@table, column], :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Column', new_table_column_path(@table)
