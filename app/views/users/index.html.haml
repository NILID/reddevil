- set_meta_tags title: t('users.title')

.row
  .col-12
    .card
      .card-body
        .lead= t 'users.title'
    %table.table.table-striped.bg-light.mt-2
      %thead
        %tr
          %th(scope='col')
          %th(scope='col')
          %th(scope='col') username
          %th(scope='col')
            = sort_link @q, :updated_at, User.human_attribute_name(:updated_at)
          %th(scope='col') reg confirm
          %th(scope='col')
      %tbody
        - @users.each do |user|
          %tr
            %th(scope="row")
              = user.id
            %td
              = image_tag user.avatar.url(:thumb), class: 'thumbnail img-fluid'
            %td
              = link_to user.email, user
              - if user.member
                .text-muted= user.fullname
            %td
              = last_seen(user)
            %td= user.confirmed?
            - if can? :edit, user
              %td
                .btn-group
                  %button.btn.btn-sm.btn-secondary.dropdown-toggle{ type: 'button', data: { toggle: 'dropdown' }, aria: { haspopup: 'true', expanded: 'false' } }
                    = fa_icon 'cog'
                  .dropdown-menu.dropdown-menu-right
                    = link_to t('profiles.edit'), edit_user_path(user), class: 'dropdown-item'
                    - if can? :edit_roles, user
                      = link_to t('users.to_edit_roles'), edit_roles_user_path(user), class: 'dropdown-item'
