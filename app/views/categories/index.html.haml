.card
  .card-header
    = t 'categories.title'
    %span.float-right
      = link_to t('shared.add'), new_category_path, class: 'btn btn-primary btn-sm'
  .card-body
    %ol= render partial: 'categories/category', collection: @root_categories
