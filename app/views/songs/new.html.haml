%h3= t 'songs.add'

= bootstrap_form_for [@album, @song], layout: :horizontal  do |f|
  = f.text_field :title
  = f.file_field :file
  = f.form_group do
    = f.primary
    = link_to t('shared.cancel'), @album, class: 'btn btn-default'