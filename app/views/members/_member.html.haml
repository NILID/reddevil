%p{ class: bdclass }
  - if bdclass
    = fa_icon 'gift', style: 'font-size:20px;margin-right:10px;'
  = l member.birth,  format: :very_short
  \ -
  = member.fullname
  (#{turntext + ' ' + plural((member.age + (should_be_age ? 1 : 0)), 'year')})