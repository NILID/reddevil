.container
  %table
    -@users.each_with_index do |user, index|
      %tr
        %td= index+=1
        %td= link_to user.email, [user, user.profile]
        %td= link_to t('profiles.edit'), edit_user_profile_path(user) if can? :edit, user.profile

