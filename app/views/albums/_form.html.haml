= form_for @album, html: {class: 'form-horizontal', multipart: true} do |f|
  - if @album.errors.any?
    #error_explanation
      %h2= "#{pluralize(@album.errors.count, "error")} prohibited this album from being saved:"
      %ul
        - @album.errors.full_messages.each do |msg|
          %li= msg

  = f.hidden_field :parent_id

  .control-group
    = f.label :title, class: 'control-label'
    .controls= f.text_field :title

  .control-group
    = f.label :songs, class: 'control-label'
    .controls= file_field_tag "songs[]", type: :file, multiple: true

  .form-actions
    = f.submit t('shared.save'), class: 'btn btn-primary'
    = link_to t('shared.cancel'), :back, class: 'btn'
