=# c.object.data.to_datetime
= c.text_field :data,
  prepend: content_tag(:i, '', class: 'glyphicon glyphicon-calendar'),
  class: 'datepicker',
  label: c.object.column.name,
  value: (c.object.data.to_datetime.strftime('%d.%m.%Y') if c.object.data.present?)
